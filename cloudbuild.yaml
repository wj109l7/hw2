steps:
  - name: 'gcr.io/cloudbuild/docker'
    args:
      - 'build'
      - '-t'
      - 'us-central1-docker.pkg.dev/${PROJECT_ID}/my-docker-repo/hw2'
      - '.'
  - name: 'gcr.io/cloudbuild/docker'
    args:
      - 'push'
      - 'us-central1-docker.pkg.dev/${PROJECT_ID}/my-docker-repo/hw2'
  - name: 'gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'hw2'
      - '--image'
      - 'uus-central1-docker.pkg.dev/${PROJECT_ID}/my-docker-repo/hw2'
      - '--platform'
      - 'managed'
      - '--region'
      - 'us-central1'
      - '--allow-unauthenticated'
      - '--port'
      - '8080' 
